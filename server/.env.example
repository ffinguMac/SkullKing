# Server runtime
NODE_ENV=production
PORT=3000

# Vercel production domain (set after first frontend deploy)
CLIENT_URL=https://your-project.vercel.app

# Optional: Supabase persistence
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Local simulation helper (optional)
SIM_ROUNDS=2
